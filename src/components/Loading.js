import React from "react";

import '../css/Loading.css';

export default function Loading() {
    return (
        <div className='body_container margin_loading'>
            <div className='loading' />
        </div>
    );
}